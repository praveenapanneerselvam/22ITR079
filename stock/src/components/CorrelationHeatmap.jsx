import { Box, Typography } from '@mui/material';

const CorrelationHeatmap = () => {
  return (
    <Box p={3}>
      <Typography variant="h5">Correlation Heatmap (Coming Soon)</Typography>
    </Box>
  );
};

export default CorrelationHeatmap;
